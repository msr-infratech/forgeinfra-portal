services:
  portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: forgeinfra-portal
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - forgeinfra

networks:
  forgeinfra:
    external: true
